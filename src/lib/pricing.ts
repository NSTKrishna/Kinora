/**
 * Where "running low" starts.
 *
 * One still costs 2 and one clip costs 20, so a balance under a clip is the
 * point at which the next thing someone tries is likely to fail. Warning then
 * — rather than at zero — is the difference between a nudge and a dead end.
 */
export const LOW_BALANCE = 20;
